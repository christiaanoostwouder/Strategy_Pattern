using UnityEngine;

public class Card : MonoBehaviour
{
    public float card_cost;
    public float card_time;
    public float card_rate;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
